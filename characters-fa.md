---
tags:
  - fa
---
[//]: # (title: Characters)

کاراکترها با نوع `Char` نمایش داده می‌شوند.

حروف (Literals) کاراکتر در تک‌گیومه قرار می‌گیرند: `'1'`.

> روی JVM، یک کاراکتر که به‌عنوان نوع اولیهٔ `char` ذخیره می‌شود، نمایانگر یک کاراکتر ۱۶ بیتی یونیکد است.


کاراکترهای خاص با بک‌اسلش `\` شروع می‌شوند.

دنباله‌های فرار (escape sequences) زیر پشتیبانی می‌شوند:

- `\t` – تب (tab)
- `\b` – پس‌گرد (backspace)
- `\n` – خط جدید (LF)
- `\r` – بازگشت به ابتدا (CR)
- `\'` – تک‌گیومه
- `\"` – جفت‌گیومه
- `\\` – بک‌اسلش
- `\$` – علامت دلار

برای کدگذاری هر کاراکتر دیگر، از سینتکس دنبالهٔ فرار یونیکد استفاده کنید: `'\uFF00'`.

```kotlin
fun main() {
//sampleStart
    val aChar: Char = 'a'
 
    println(aChar)
    println('\n') // Prints an extra newline character
    println('\uFF00')
//sampleEnd
}
```


اگر مقدار یک متغیر کاراکتری یک رقم باشد، می‌توانید آن را به‌طور صریح با استفاده از تابع [`digitToInt()`](https://kotlinlang.org/api/latest/jvm/stdlib/kotlin.text/digit-to-int.html) به یک عدد `Int` تبدیل کنید.

> روی ماشین مجازی جاوا، کاراکترها وقتی نیاز به ارجاع nullable باشد، در کلاس‌های جاوا box می‌شوند، درست مانند [اعداد](https://numbers.md/#boxing-and-caching-numbers-on-the-java-virtual-machine).  
> هویت توسط عملیات boxing حفظ نمی‌شود.



---
# محتوای شخصی:

### «روی JVM، یک کاراکتر که به‌عنوان نوع اولیهٔ `char` ذخیره می‌شود، نمایانگر یک کاراکتر ۱۶ بیتی یونیکد است» یعنی چی؟

**پاسخ ساده:**  
یعنی هر متغیر `char` در Kotlin (روی JVM) می‌تواند هر یک از کاراکترهای یونیکد را که در محدوده‌ی ۰ تا ۶۵۵۳۵ باشد، ذخیره کند. برای این کار از ۱۶ بیت (۲ بایت) حافظه استفاده می‌شود.

**توضیح بیشتر:**  
یونیکد استانداردی است که به هر حرف، رقم، علامت یا شکلک یک عدد منحصربه‌فرد به اسم «کدپوینت» می‌دهد.  
محدوده‌ی اول یونیکد (Basic Multilingual Plane) شامل ۶۵۵۳۶ کاراکتر است که با ۱۶ بیت قابل نمایش هستند. `char` در جاوا و کاتلین روی JVM دقیقاً این محدوده را پشتیبانی می‌کند.  
برای مثال `'A'`، `'۱'`، `'😊'` (که این یکی در واقع خارج از ۱۶ بیت است و با دو `char` نمایش داده می‌شود).

> خلاصه: `char` یک خانه‌ی ۲ بایتی است که می‌تواند بیشتر کاراکترهای روزمره را در خود جای دهد.

### «روی ماشین مجازی جاوا، کاراکترها وقتی نیاز به ارجاع nullable باشد، در کلاس‌های جاوا box می‌شوند، درست مانند اعداد» یعنی چی؟

**پاسخ ساده:**  
یعنی وقتی `Char` را به صورت nullable تعریف می‌کنید (مثلاً `Char?`)، دیگر مستقیماً در حافظه به عنوان یک مقدار اولیه (primitive) ذخیره نمی‌شود، بلکه داخل یک جعبه (شیء از جنس کلاس `Character` در جاوا) قرار می‌گیرد تا بتواند `null` هم بگیرد.

**مثال:**
```kotlin
val a: Char = 'a'     // این primitive ذخیره می‌شود (کارآمد)
val b: Char? = 'b'    // این box می‌شود (شیء ساخته می‌شود)
val c: Char? = null   // null فقط با حالت box شده ممکن است
```

دلیل: نوع‌های اولیه مثل `char` در JVM نمی‌توانند مقدار `null` داشته باشند. برای اینکه `null` پشتیبانی شود، مجبوریم از کلاس‌های wrapper مثل `Character` استفاده کنیم که شیء هستند و می‌توانند نال باشند.

