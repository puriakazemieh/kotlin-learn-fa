---
tags:
  - fa
---
[//]: # (title: Visibility modifiers)

کلاس‌ها، آبجکت‌ها، اینترفیس‌ها، سازنده‌ها و توابع، و همچنین پراپرتی‌ها و setterهای آن‌ها می‌توانند _اصلاح‌کننده‌های سطح دسترسی_ داشته باشند.  

در Getterها همیشه همان سطح دسترسیِ پراپرتیِ خودشان را دارند.

در کاتلین چهار اصلاح‌کنندهٔ سطح دسترسی وجود دارد: `private`، `protected`، `internal` و `public`.  
سطح دسترسی پیش‌فرض `public` است.

در این صفحه یاد می‌گیرید که این اصلاح‌کننده‌ها در انواع مختلفِ scopeهای اعلان (declaring scopes) چگونه اعمال می‌شوند.

## پکیج‌ها

توابع، پراپرتی‌ها، کلاس‌ها، آبجکت‌ها و اینترفیس‌ها می‌توانند در سطح بالا (top-level) مستقیماً داخل یک پکیج اعلان شوند:

```kotlin
// file name: example.kt
package foo

fun baz() { ... }
class Bar { ... }
```

- اگر از اصلاح‌کنندهٔ سطح دسترسی استفاده نکنید، به‌صورت پیش‌فرض `public` استفاده می‌شود؛ یعنی اعلان‌های شما همه‌جا قابل مشاهده خواهند بود.
- اگر یک اعلان را `private` کنید، فقط داخل همان فایلی که اعلان در آن قرار دارد قابل مشاهده است.
- اگر آن را `internal` کنید، در همه‌جای همان [ماژول](#modules) قابل مشاهده خواهد بود.
- اصلاح‌کنندهٔ `protected` برای اعلان‌های top-level در دسترس نیست.

> برای استفاده از یک اعلانِ top-level که قابل مشاهده است از یک پکیج دیگر، باید آن را [import](packages-fa.md#imports) کنید.

مثال‌ها:

```kotlin
// file name: example.kt
package foo

private fun foo() { ... } // فقط داخل example.kt قابل مشاهده است

public var bar: Int = 5 // پراپرتی همه‌جا قابل مشاهده است
    private set         // setter فقط در example.kt قابل مشاهده است
    
internal val baz = 6    // فقط داخل همان ماژول قابل مشاهده است

```

## اعضای کلاس

برای اعضایی که داخل یک کلاس اعلان می‌شوند:

- در`private` یعنی عضو فقط داخل همین کلاس قابل مشاهده است (از جمله همهٔ اعضای آن).
- در`protected` یعنی عضو همان سطح دسترسیِ `private` را دارد، اما علاوه بر آن در زیرکلاس‌ها (subclass) هم قابل مشاهده است.
- در`internal` یعنی هر کلاینتی _داخل همین ماژول_ که کلاسِ اعلان‌کننده را می‌بیند، اعضای `internal` آن را هم می‌بیند.
- در`public` یعنی هر کلاینتی که کلاسِ اعلان‌کننده را می‌بیند، اعضای `public` آن را هم می‌بیند.

> در کاتلین، کلاس بیرونی (outer class) اعضای private کلاس‌های درونی (inner classes) خود را نمی‌بیند.

اگر یک عضو `protected` یا `internal` را override کنید و سطح دسترسی را صراحتاً مشخص نکنید، عضو override‌شده هم همان سطح دسترسیِ عضو اصلی را خواهد داشت.

مثال‌ها:

```kotlin
open class Outer {
    private val a = 1
    protected open val b = 2
    internal open val c = 3
    val d = 4  // به‌صورت پیش‌فرض public
    
    protected class Nested {
        public val e: Int = 5
    }
}

class Subclass : Outer() {
    // a قابل مشاهده نیست
    // b، c و d قابل مشاهده‌اند
    // Nested و e قابل مشاهده‌اند

    override val b = 5   // 'b' protected است
    override val c = 7   // 'c' internal است
}

class Unrelated(o: Outer) {
    // o.a و o.b قابل مشاهده نیستند
    // o.c و o.d قابل مشاهده‌اند (در همان ماژول)
    // Outer.Nested قابل مشاهده نیست، و Nested::e هم قابل مشاهده نیست
}

```

### سازنده‌ها

برای تعیین سطح دسترسیِ سازندهٔ اصلی (primary constructor) یک کلاس، از نحو زیر استفاده کنید:

> باید کلمهٔ کلیدی `constructor` را به‌صورت صریح اضافه کنید.

```kotlin
class C private constructor(a: Int) { ... }
```

اینجا سازنده `private` است. به‌صورت پیش‌فرض، همهٔ سازنده‌ها `public` هستند؛ که عملاً یعنی هرجا خودِ کلاس قابل مشاهده باشد، سازنده هم قابل مشاهده است (این یعنی سازندهٔ یک کلاس `internal` فقط داخل همان ماژول قابل مشاهده است).

برای sealed classها، سازنده‌ها به‌صورت پیش‌فرض `protected` هستند. برای اطلاعات بیشتر، [Sealed classes](sealed-classes.md#constructors) را ببینید.
### اعلان‌های محلی

متغیرها، توابع و کلاس‌های محلی (local) نمی‌توانند اصلاح‌کنندهٔ سطح دسترسی داشته باشند.

## ماژول‌ها

اصلاح‌کنندهٔ سطح دسترسی `internal` یعنی عضو فقط داخل همان ماژول قابل مشاهده است. دقیق‌تر، ماژول مجموعه‌ای از فایل‌های کاتلین است که با هم کامپایل می‌شوند؛ مثلاً:

- یک ماژول در IntelliJ IDEA.
- یک پروژهٔ Maven.
- یک source set در Gradle (با این استثنا که source set مربوط به `test` می‌تواند به اعلان‌های internal مربوط به `main` دسترسی داشته باشد).
- مجموعه‌ای از فایل‌ها که با یک اجرای task آنتِ `<kotlinc>` کامپایل می‌شوند.



---
# محتوای شخصی:

### `protected` دقیقاً یعنی چی؟

در واقع `protected` یعنی **فقط داخل همان کلاس و داخل زیرکلاس‌ها** قابل دسترسی است.

- از **خودِ کلاس** قابل دسترسی است.
- از **کلاس‌های فرزند (subclass)** هم قابل دسترسی است.
- اما از **بیرونِ سلسله‌مراتب ارث‌بری** (حتی اگر داخل همان ماژول باشد) قابل دسترسی نیست.
- در Kotlin، `protected` برای **اعلان‌های top-level** (بیرون از کلاس) وجود ندارد؛ فقط برای **اعضای کلاس** معنی دارد.

مثال ساده:

```kotlin
open class A {
    protected val x = 10
}

class B : A() {
    fun f() = x        // ✅ چون B زیرکلاس A است
}

fun main() {
    val a = A()
    // a.x              // ❌ خطا: بیرون از کلاس/زیرکلاس است

    val b = B()
    // b.x              // ❌ حتی با اینکه b از نوع B است، باز هم بیرونِ کلاس B هستیم
}

```

نکته مهم:  
در `protected` یعنی «از بیرون با آبجکت نتون ببین»، حتی اگر اون آبجکت از نوع زیرکلاس باشه. دسترسی protected فقط **از داخل بدنهٔ کلاس/زیرکلاس‌ها** مجازه، نه از بیرون با `obj.member`.

#### مقایسه سریع با Java

در **Java**، `protected` علاوه بر زیرکلاس‌ها، برای **کلاس‌های هم‌پکیج (package)** هم قابل دسترسیه.  
اما در **Kotlin** این رفتارِ “package-protected” وجود نداره؛ `protected` فقط برای ارث‌بریه.

### local

منظورش از **local** یعنی چیزی که **داخل یک تابع یا یک بلاک** تعریف شده، نه داخل کلاس و نه top-level.

مثلاً این‌ها محلی (local) هستند:
```kotlin
fun demo() {
    val a = 1              // local variable
    fun inner() { }        // local function
    class LocalClass { }   // local class
}

```
حالا چرا نمی‌تونن `private/public/protected/internal` داشته باشن؟

چون این چیزها **از قبل فقط در همان محدوده (scope) قابل دسترسی‌اند**؛ یعنی بیرون از `demo()` اصولاً وجود ندارن که بخوای سطح دسترسی‌شون رو کنترل کنی.

پس این‌ها **غلط** هستند:
```kotlin
fun demo() {
    private val a = 1      // ❌ خطا
    internal fun inner() {} // ❌ خطا
    protected class C {}    // ❌ خطا
}

```
در واقع:

- local ها ذاتاً “محدود به همان بلاک/تابع” هستند،
- بنابراین modifierهای visibility براشون معنی نداره و Kotlin اجازه نمی‌ده.

اگر دوست داری، می‌تونم با چند مثال «داخل فایل»، «داخل کلاس»، و «داخل تابع» کنار هم نشون بدم که دقیقاً scopeها چطور فرق می‌کنن.