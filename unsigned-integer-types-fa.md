---
tags:
  - fa
---
[//]: # (title: Unsigned integer types)

علاوه بر [نوع‌های صحیح](numbers-fa.md#integer-types)، کاتلین نوع‌های زیر را برای اعداد صحیح بدون علامت فراهم می‌کند:

| Type     | Size (bits) | Min value | Max value                                       |
|----------|-------------|-----------|-------------------------------------------------|
| `UByte`  | 8           | 0         | 255                                             |
| `UShort` | 16          | 0         | 65,535                                          |
| `UInt`   | 32          | 0         | 4,294,967,295 (2<sup>32</sup> - 1)              |
| `ULong`  | 64          | 0         | 18,446,744,073,709,551,615 (2<sup>64</sup> - 1) |


نوع‌های بدون علامت از بیشتر عملیات‌های معادل‌های با علامت خود پشتیبانی می‌کنند.

> اعداد بدون علامت به‌عنوان [inline class](inline-classes.md) با یک ویژگی ذخیره‌سازی منفرد پیاده‌سازی می‌شوند که نوع صحیح با علامت متناظر با همان عرض را نگه می‌دارد. اگر می‌خواهید بین نوع‌های صحیح بدون علامت و با علامت تبدیل انجام دهید، مطمئن شوید کد را طوری به‌روز کنید که فراخوانی تابع‌ها و عملیات‌ها از نوع جدید پشتیبانی کنند.

## آرایه‌ها و بازه‌های بدون علامت

> آرایه‌های بدون علامت و عملیات‌های مربوط به آن‌ها در مرحلهٔ [Beta](components-stability.md) قرار دارند. ممکن است هر زمان ناسازگار تغییر کنند.
> 
> برای استفاده باید opt-in انجام دهید (جزئیات در ادامه).

همانند انواع اولیه، هر نوع بدون علامت یک نوع متناظر دارد که آرایه‌ای از آن نوع را نمایش می‌دهد:

- `UByteArray`: آرایه‌ای از بایت‌های بدون علامت
- `UShortArray`: آرایه‌ای از شورت‌های بدون علامت
- `UIntArray`: آرایه‌ای از انت‌های بدون علامت
- `ULongArray`: آرایه‌ای از لانگ‌های بدون علامت

مانند آرایه‌های صحیح با علامت، این آرایه‌ها نیز API مشابه کلاس `Array` را بدون سربار boxing ارائه می‌دهند.

وقتی از آرایه‌های بدون علامت استفاده می‌کنید، هشداری دریافت می‌کنید که نشان می‌دهد این قابلیت هنوز پایدار نیست.

برای حذف این هشدار، با annotation‏ `@ExperimentalUnsignedTypes` opt-in کنید.

این تصمیم با شماست که آیا کاربران API شما باید به‌صورت صریح opt-in کنند یا نه، اما در نظر داشته باشید که آرایه‌های بدون علامت هنوز پایدار نیستند، بنابراین APIای که از آن‌ها استفاده می‌کند ممکن است با تغییرات زبان دچار شکست شود.

[دربارهٔ نیازهای opt-in بیشتر بدانید](opt-in-requirements.md).

[بازه‌ها و progressionها](ranges.md) برای `UInt` و `ULong` توسط کلاس‌های `UIntRange`، `UIntProgression`، `ULongRange` و `ULongProgression` پشتیبانی می‌شوند. همراه با نوع‌های صحیح بدون علامت، این کلاس‌ها پایدار هستند.

## لیترال‌های اعداد بدون علامت

برای آسان‌تر شدن استفاده از اعداد بدون علامت، می‌توانید یک پسوند به یک لیترال عددی اضافه کنید

که نوع بدون علامت خاصی را مشخص می‌کند (مشابه `F` برای `Float` یا `L` برای `Long`):

- حروف `u` و `U` نشان‌دهندهٔ لیترال‌های بدون علامت بدون تعیین دقیق نوع هستند.اگر نوع مورد انتظار مشخص نشده باشد، کامپایلر بسته به اندازهٔ ثابت، از `UInt` یا `ULong` استفاده می‌کند:

    ```kotlin
    val b: UByte = 1u  // UByte، نوع مورد انتظار مشخص است
    val s: UShort = 1u // UShort، نوع مورد انتظار مشخص است
    val l: ULong = 1u  // ULong، نوع مورد انتظار مشخص است
  
    val a1 = 42u // UInt: نوع مورد انتظار وجود ندارد، مقدار در UInt جا می‌شود
    val a2 = 0xFFFF_FFFF_FFFFu // ULong: مقدار در UInt جا نمی‌شود

    ```

* `uL` و `UL` به‌صورت صریح تعیین می‌کنند که لیترال باید یک unsigned long باشد:

    ```kotlin
	val a = 1UL // ULong، حتی اگر نوع مورد انتظار مشخص نشده و مقدار در UInt جا می‌شود

    ```

## کاربردها

کاربرد اصلی اعداد بدون علامت استفادهٔ کامل از بازهٔ بیتی یک عدد صحیح برای نمایش مقادیر مثبت است.

برای نمونه، برای نمایش ثابت‌های هگزادسیمال که در نوع‌های با علامت جا نمی‌شوند، مثل رنگ در قالب 32 بیتی `AARRGGBB`:

```kotlin
data class Color(val representation: UInt)

val yellow = Color(0xFFCC00CCu)
```

می‌توانید از اعداد بدون علامت برای مقداردهی آرایه‌های بایت بدون نیاز به `toByte()` استفاده کنید:

```kotlin
val byteOrderMarkUtf8 = ubyteArrayOf(0xEFu, 0xBBu, 0xBFu)
```

کاربرد دیگر، سازگاری با APIهای native است. کاتلین اجازه می‌دهد اعلان‌های native که شامل نوع‌های بدون علامت هستند نمایش داده شوند. این نگاشت نوع‌های بدون علامت را با نوع‌های با علامت جایگزین نمی‌کند و معنا را تغییر نمی‌دهد.

### اهداف‌نبودن (Non-goals)

با اینکه عددهای بدون علامت فقط مقادیر مثبت و صفر را نمایش می‌دهند، هدف این نیست که در هر جایی که دامنهٔ کاربرد اعداد نامنفی نیاز است از آن‌ها استفاده شود.

برای مثال، به‌عنوان نوع اندازهٔ مجموعه یا اندیس مجموعه.

چند دلیل وجود دارد:

- استفاده از نوع‌های صحیح با علامت می‌تواند به شناسایی سرریزهای ناخواسته و علامت‌دهی شرایط خطا کمک کند،مانند مقدار -1 برای [`List.lastIndex`](https://kotlinlang.org/api/latest/jvm/stdlib/kotlin.collections/last-index.html) در لیست خالی.
- نوع‌های بدون علامت نمی‌توانند نسخهٔ محدودشدهٔ نوع‌های با علامت باشند، زیرا دامنهٔ مقادیر آن‌ها زیرمجموعهٔ نوع‌های با علامت نیست.نه نوع‌های با علامت و نه نوع‌های بدون علامت زیرنوع یکدیگر نیستند.



---
# محتوای شخصی:

این نوع‌ها زمانی به درد می‌خورند که **نیاز داری از تمام ظرفیت بیت‌های یک عدد برای نمایش مقدارهای مثبت استفاده کنی** و نمی‌خواهی نصف محدوده به نمایش اعداد منفی اختصاص پیدا کند. اجازه بده چند مثال واقعی و ملموس بزنم که دقیقاً کجا کاربرد پیدا می‌کنند.

## 1) **نمایش مقدارهایی که ذاتاً منفی نیستند** (مثل رنگ‌ها، بیت‌فلگ‌ها، دادهٔ باینری)

وقتی با داده‌ای سروکار داری که منفی نمی‌شود، مثل رنگ‌ها در فرمت 32 بیت:

اگر از `Int` (با علامت) استفاده کنی، نصف محدوده‌اش مخصوص عددهای منفی است و ممکن است مقدار رنگ به‌عنوان یک مقدار منفی نمایش داده شود که گیج‌کننده است.

```kotlin
AARRGGBB
```

ولی با `UInt` همیشه مقدار بین 0 و ‎4,294,967,295‎ است.

مثال:
```kotlin
data class Color(val value: UInt)

val yellow = Color(0xFFCC00CCu)

```

## 2) **کار با داده‌های باینری و پروتکل‌ها**

در پروتکل‌ها و فایل‌های باینری، اغلب داده‌ها “بایت” هستند

و بایت معمولاً بین 0 تا 255 است، نه -128 تا +127.

در کاتلین:

- `Byte` مقدارهای منفی دارد
- `UByte` دقیقاً همان چیزی است که در باینری استاندارد لازم داریم

مثلاً فایل UTF‑8 BOM:

```kotlin
val bom = ubyteArrayOf(0xEFu, 0xBBu, 0xBFu)

```

اگر از `Byte` استفاده کنی باید بنویسی:

```kotlin
val bom = byteArrayOf(0xEF.toByte(), 0xBB.toByte(), 0xBF.toByte())

```

کاملاً زجرآور.

## 3) **کار با سخت‌افزار یا سیستم‌هایی که unsigned دارند**

زبان‌هایی مثل C، C++، Rust، Swift همیشه نوع‌های unsigned دارند

و کاتلین/native گاهی باید با آن‌ها صحبت کند.

برای مثال، اگر داری با Arduino، IoT یا APIهای سطح پایین کار می‌کنی،

یک مقدار 16 بیتی unsigned از C می‌آید:

```kotlin
unsigned short read_sensor_value();

```


در کاتلین این باید تبدیل شود به:

```kotlin
fun readSensor(): UShort

```

اگر نوع signed بگذاری مثل `Short`، داده خراب می‌شود چون نصف مقادیرش منفی هستند.

## 4) **استفادهٔ کامل از ظرفیت یک عدد برای شمارش**

اگر می‌خواهی از یک عدد 32 بیت برای شمارش مقادیر مثبت استفاده کنی،

`UInt` دو برابر مقدار قابل نمایش دارد نسبت به `Int`.

- `Int`: حداکثر ‎2,147,483,647‎
- `UInt`: حداکثر ‎4,294,967,295‎

برای شمارنده‌های بزرگ، IDهای دیتا، نسخه‌ها، هش‌ها و… این مهم است.

مثال:

```kotlin
var packetCounter: UInt = 0u
packetCounter++

```

## 5) **وقتی مقدار منفی اصلاً معنا ندارد**

مثلاً:

- شماره رنگ
- اندازهٔ پیکسل
- آی‌دی‌های دیتا
- مقادیر هگز
- داده‌های رمزنگاری
- ماسک‌های بیتی

این‌ها منفی نیستند و نباید باشند.

## 6) **امنیت و رمزنگاری (Crypto)**

رمزنگاری پر از عملیات بیتی و بایت‌هاست.

و unsigned مناسب‌ترین حالت برای این جور داده‌هاست.

مثال ساده‌ی XOR روی بایت‌ها:

```kotlin
fun xor(a: UByte, b: UByte): UByte = (a xor b)

```

با `Byte` باید مراقب مقادیر منفی باشی و عملیات پیچیده می‌شود.