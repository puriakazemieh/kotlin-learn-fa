---
tags:
  - fa
---
[//]: # (title: Booleans)

نوع `Boolean` نمایانگر شیءهای بولی است که می‌توانند دو مقدار داشته باشند: `true` و `false`.

نوع `Boolean` یک نسخهٔ [nullable](null-safety.md) نیز دارد که به صورت `Boolean?` تعریف شده است.

> روی JVM، بولین‌هایی که به‌صورت نوع اولیهٔ `boolean` ذخیره می‌شوند معمولاً از ۸ بیت استفاده می‌کنند.


عملیات‌های داخلی روی بولین‌ها شامل موارد زیر است:

- `||` – فصل (OR منطقی)
- `&&` – وصل (AND منطقی)
- `!` – نقیض (NOT منطقی)

برای مثال:

```kotlin
fun main() {
//sampleStart
    val myTrue: Boolean = true
    val myFalse: Boolean = false
    val boolNull: Boolean? = null

    println(myTrue || myFalse)
    // true
    println(myTrue && myFalse)
    // false
    println(!myTrue)
    // false
    println(boolNull)
    // null
//sampleEnd
}
```

عملگرهای `||` و `&&` به‌صورت تنبل (lazy) عمل می‌کنند، یعنی:

- اگر عملوند اول `true` باشد، عملگر `||` عملوند دوم را ارزیابی نمی‌کند.
- اگر عملوند اول `false` باشد، عملگر `&&` عملوند دوم را ارزیابی نمی‌کند.

> روی JVM، ارجاع‌های nullable به شیءهای بولین در کلاس‌های Java باکس می‌شوند، درست مانند [اعداد](numbers.md#boxing-and-caching-numbers-on-the-java-virtual-machine).


---
# محتوای شخصی:

### «روی JVM، بولین‌هایی که به‌صورت نوع اولیهٔ `boolean` ذخیره می‌شوند معمولاً از ۸ بیت استفاده می‌کنند» یعنی چی؟

**پاسخ ساده:**  
یعنی در حافظه، برای ذخیره‌ی یک `true` یا `false`، معمولاً ۱ بایت (که برابر ۸ بیت است) فضا گرفته می‌شود، نه فقط ۱ بیت.

**توضیح فنی‌تر:**  
در سطح منطقی، یک بولین فقط دو مقدار دارد، که با ۱ بیت هم قابل ذخیره‌سازی است.  
اما معماری ماشین‌های مجازی و پردازنده‌ها طوری طراحی شده که به آدرس‌دهی بایت‌ها (گروه‌های ۸ بیتی) عادت دارند. خوندن و نوشتن یک بایت به تنهایی ساده‌تر و سریع‌تر از دستکاری یک بیت منفرد است.  
به همین دلیل، JVM معمولاً از یک بایت کامل برای ذخیره‌ی بولین استفاده می‌کند، هرچند از نظر استاندارد این وابسته به پیاده‌سازی است.

> خلاصه: بولین از نظر منطقی ۱ بیت است، اما در عمل روی JVM تقریباً همیشه ۸ بیت (۱ بایت) حافظه اشغال می‌کند.

